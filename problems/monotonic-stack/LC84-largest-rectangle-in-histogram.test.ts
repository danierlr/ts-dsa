// function largestRectangleArea(heights: number[]): number {
// 	const smallerHeightIndexes: number[] = []

// 	let maxArea = 0

// 	for(let index = 0; index < heights.length; index += 1){
// 		const numSmaller = smallerHeightIndexes.length

// 		let prevTopIndex = 0

// 		while(smallerHeightIndexes.length > 0 && heights[index]! < heights[smallerHeightIndexes[numSmaller - 1]!]!) {
// 			const prevIndex = smallerHeightIndexes[numSmaller - 1]!
// 			const prevHeight = heights[prevIndex]!

			
// 		}
// 	}
// }



describe('tests', () => {
  it('desc 0', () => {
    expect(1).toEqual(1)
  })
})